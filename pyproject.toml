[project]
name = "lastfm-sentiment-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.7"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.26.0",
]
